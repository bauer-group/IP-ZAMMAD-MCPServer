name: 🚀 Release & Docker Build

on:
  push:
    branches: [main]
    paths-ignore:
      - '.github/**'
      - '*.md'
      - 'docs/**'
  pull_request:
    branches: [main]
    paths:
      - 'app/**'
      - 'docker-compose*.yml'
      - '.dockerignore'
      - '.env.example'
  workflow_dispatch:
    inputs:
      force-release:
        description: 'force create release'
        type: boolean
        default: false

permissions:
  contents: write
  issues: write
  pull-requests: write
  packages: write
  security-events: write
  # Required by docker-build.yml@main: a declared permissions: block sets every
  # unlisted scope to none, and the reusable workflow now needs these three.
  attestations: write   # write the build-provenance attestation
  id-token: write       # OIDC token to sign the attestation (keyless/Sigstore)
  actions: read         # its nested security-analysis job reads run metadata

jobs:
  # ============================================
  # Validation Jobs
  # ============================================

  validate-compose:
    name: 🔍 Validate Docker Compose
    uses: bauer-group/automation-templates/.github/workflows/modules-validate-compose.yml@main
    with:
      compose-files: '["docker-compose.coolify.yml", "docker-compose.traefik.yml", "docker-compose.development.yml"]'
      env-file: '.env.example'
      validate-services: '["zammad-mcp", "redis"]'

  # The Python gate: ruff + mypy --strict + pytest with coverage enforced.
  # Listed in the release job's `needs` below, so no tag, GitHub Release or
  # CHANGELOG entry can be cut while the suite is red.
  quality:
    name: 🧪 Tests
    uses: ./.github/workflows/tests.yml

  # ============================================
  # Release Job (only on main branch push)
  # ============================================

  release:
    name: 📦 Create Semantic Release
    needs: [validate-compose, quality]
    if: |
      (github.event_name == 'push' || github.event_name == 'workflow_dispatch') &&
      needs.validate-compose.result == 'success' &&
      needs.quality.result == 'success'
    uses: bauer-group/automation-templates/.github/workflows/modules-semantic-release.yml@main
    with:
      target-branch: 'main'
      dry-run: false
      force-release: ${{ inputs.force-release || false }}
      extra-plugins: '@semantic-release/exec'
    secrets: inherit

  # ============================================
  # Docker Build & Push (bg-zammad-mcp image)
  # ============================================

  docker-build-bg-zammad-mcp-release:
    name: 🐳 Build & Push bg-zammad-mcp
    needs: release
    if: needs.release.outputs.release-created == 'true'
    uses: bauer-group/automation-templates/.github/workflows/docker-build.yml@main
    with:
      deploy-environment: 'production'
      publish-to: 'ghcr'
      ghcr-image-name: 'bauer-group/IP-ZAMMAD-MCPServer/bg-zammad-mcp'
      docker-image-name: 'bauergroup/bg-zammad-mcp'

      release-version: ${{ needs.release.outputs.version }}
      image-tags: 'stable'

      update-dockerfile-version: true
      auto-tags: true
      latest-tag: true

      dockerfile-path: './app/bg-zammad-mcp/Dockerfile'
      docker-context: './app/bg-zammad-mcp'
      platforms: 'linux/amd64,linux/arm64'

      push: true

      # Disabled again after v1.0.0. Enabling it does NOT block a bad image —
      # the scan runs AFTER the push (the v1.0.0 image reached GHCR at 14:38:22,
      # the job failed at 14:38:38) — and its SARIF upload step in
      # automation-templates exits 128, a git error, on every run. The result was
      # a permanently red release job that reports nothing, which is worse than
      # no scan: it trains everyone to ignore the pipeline.
      #
      # security-fail-on and the security-events permission are kept so this is a
      # one-line re-enable once the template's upload step is fixed upstream.
      security-scan: false
      security-fail-on: 'CRITICAL'
      generate-sbom: true
      sync-dockerhub-readme: true
    secrets: inherit

  docker-build-bg-zammad-mcp-pr:
    name: 🔨 Validate bg-zammad-mcp Build (PR)
    needs: [validate-compose, quality]
    if: |
      github.event_name == 'pull_request' &&
      needs.validate-compose.result == 'success' &&
      needs.quality.result == 'success'
    uses: bauer-group/automation-templates/.github/workflows/docker-build.yml@main
    with:
      publish-to: 'ghcr'
      ghcr-image-name: 'bauer-group/IP-ZAMMAD-MCPServer/bg-zammad-mcp'

      auto-tags: true
      dockerfile-path: './app/bg-zammad-mcp/Dockerfile'
      docker-context: './app/bg-zammad-mcp'
      platforms: 'linux/amd64,linux/arm64'

      push: false

      security-scan: false
      security-fail-on: 'CRITICAL'
    secrets: inherit
